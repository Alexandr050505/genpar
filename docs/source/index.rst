Password Generator CLI
======================

.. image:: https://img.shields.io/badge/Python-3.8%2B-blue
   :target: https://python.org
.. image:: https://img.shields.io/badge/License-MIT-green
   :target: LICENSE

**Мощный генератор паролей с безопасным хранением и CLI-интерфейсом**

.. contents:: Оглавление
   :depth: 3
   :local:

Быстрый старт
-------------

Установка
~~~~~~~~~

.. code-block:: bash

   git clone https://github.com/ваш-username/genpar.git
   cd genpar

Основные команды
~~~~~~~~~~~~~~~~

.. code-block:: bash

   # Генерация пароля
   python main.py --generate --length 16 --uppercase --digits --special

   # Сохранение пароля
   python main.py --generate --save --service gmail --login user@mail.com

   # Поиск пароля
   python main.py --find gmail

Возможности
-----------

✅ **Безопасная генерация**
   - Настраиваемая длина пароля
   - Использование заглавных букв, цифр, спецсимволов
   - Случайная выборка из пула символов

✅ **Безопасное хранение**
   - Локальное хранение в JSON-формате
   - Привязка паролей к сервисам и логинам
   - Быстрый поиск по базе

✅ **Удобный CLI**
   - Простые и понятные команды
   - Короткие алиасы для частых операций
   - Подробная справка

Примеры использования
---------------------

Базовые сценарии
~~~~~~~~~~~~~~~~

**Пример 1: Простая генерация**

.. code-block:: bash

   python main.py --generate

   # Результат: k8f#m2pL

**Пример 2: Сложный пароль для банка**

.. code-block:: bash

   python main.py -g -l 20 -u -d -s

   # Результат: @^UF270SXb6nQaw8!k9P

**Пример 3: Сохранение пароля для соцсети**

.. code-block:: bash

   python main.py -g -l 16 -u -d --save --service vk --login mylogin

Продвинутые сценарии
~~~~~~~~~~~~~~~~~~~~

**Пакетная генерация нескольких паролей:**

.. code-block:: bash

   # Для Windows CMD
   for /L %i in (1,1,5) do @python main.py --generate --length 12

Аргументы командной строки
--------------------------

+------------------+-----------+-------------------+-----------------------------------+
| Аргумент         | Алиас     | Значение          | Описание                          |
+==================+===========+===================+===================================+
| --generate       | -g        | флаг              | Генерировать пароль               |
+------------------+-----------+-------------------+-----------------------------------+
| --length         | -l        | число (12)        | Длина пароля                      |
+------------------+-----------+-------------------+-----------------------------------+
| --uppercase      | -u        | флаг              | Добавить заглавные буквы          |
+------------------+-----------+-------------------+-----------------------------------+
| --digits         | -d        | флаг              | Добавить цифры                    |
+------------------+-----------+-------------------+-----------------------------------+
| --special        | -s        | флаг              | Добавить спецсимволы              |
+------------------+-----------+-------------------+-----------------------------------+
| --save           |           | флаг              | Сохранить пароль                  |
+------------------+-----------+-------------------+-----------------------------------+
| --service        |           | строка            | Название сервиса                  |
+------------------+-----------+-------------------+-----------------------------------+
| --login          |           | строка            | Логин/email                       |
+------------------+-----------+-------------------+-----------------------------------+
| --find           |           | строка            | Найти пароль по сервису           |
+------------------+-----------+-------------------+-----------------------------------+

Структура проекта
-----------------

.. code-block::

   genpar/
   ├── main.py                 # Главный скрипт
   ├── passgen/               # Пакет генератора
   │   ├── __init__.py
   │   ├── commands.py        # Обработка команд
   │   ├── generator.py       # Генерация паролей
   │   ├── storage.py         # Работа с хранилищем
   │   └── utils.py           # Вспомогательные функции
   ├── docs/                  # Документация
   │   ├── source/
   │   └── build/
   └── passwords.json         # База паролей (создается автоматически)

Безопасность
------------

🔒 **Рекомендации по использованию:**

- Никогда не коммитьте файл `passwords.json` в Git
- Используйте длинные пароли (16+ символов)
- Включайте все типы символов для максимальной безопасности
- Храните backup файла `passwords.json` в безопасном месте

📊 **Оценка сложности пароля:**

Программа оценивает пароли по шкале от 0 до 4:

- 1 балл за длину ≥ 8 символов
- 1 балл за заглавные буквы
- 1 балл за цифры
- 1 балл за спецсимволы

Лицензия
--------

MIT License - смотри файл `LICENSE` для подробностей.

Разработчик
-----------

- **Александр** - [GitHub](https://github.com/ваш-username)

.. note::
   Для предложений и баг-репортов создавайте issue на GitHub.

.. warning::
   Не забывайте делать backup вашей базы паролей!